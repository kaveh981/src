# A Schema defining a valid array of "Proposal" objects for bulk import
#
title: proposalArray
type: array
items:
    title: proposal
    description: A JSON schema defining how a valid proposal instance should be represented
    type: object
    properties:
        proposalID:
            $ref: "#/definitions/positiveMediumInt"
        ownerID:
            $ref: "#/definitions/positiveMediumInt"
        name:
            $ref: "#/definitions/varchar255"
        description:
            $ref: "#/definitions/varchar255"
        status:
            enum: [active, paused, deleted]
        accessMode:
            $ref: "#/definitions/positiveTinyInt"
        startDate:
            $ref: "#/definitions/dateString"
        endDate:
            $ref: "#/definitions/dateString"
        price:
            type: number
            minimum: 0.01
            maximum: 655.35
        impressions:
            $ref: "#/definitions/positiveMediumInt"
        budget:
            $ref: "#/definitions/positiveMediumInt"
        auctionType:
            enum: [fixed, first, second]
        terms:
            type: string
            minLength: 1
            maxLength: 65535
        createDate:
            type: "null"
        modifyDate:
            type: "null"
        sectionIDs:
            type: array
            items:
                $ref: "#/definitions/positiveMediumInt"
            minItems: 1
            uniqueItems: true
    required: [ownerID, name, description, status, accessMode, startDate, endDate, price, impressions, budget, auctionType,
    terms, createDate, modifyDate, sectionIDs]
definitions:
    positiveMediumInt:
        type: integer
        minimum: 1
        maximum: 16777215
    positiveTinyInt:
        type: integer
        minimum: 1
        maximum: 255
    varchar255:
        type: string
        maxLength: 255
    dateString:
        type: string
        pattern: ^[0-2]\d{3}-(?:0[1-9]|1[0-2]|[1-9])-(?:[0-3]\d|[1-9])$